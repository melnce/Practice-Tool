{
  "name": "practice-tool",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "imports": {
    "#core/*": "./dist/core/*.js",
    "#logic/*": "./dist/logic/*.js",
    "#ui/*": "./dist/ui/*.js",
    "#helpers/*": "./dist/helpers/*.js",
    "#data/*": "./dist/data/*.js"
  },
  "main": "main.js",
  "scripts": {
    "test": "npm run build || exit 0 && vitest run",
    "test:watch": "npm run build || exit 0 && vitest",
    "build": "tsc",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}